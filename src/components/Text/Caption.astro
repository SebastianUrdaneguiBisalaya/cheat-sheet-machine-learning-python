---
interface CaptionProps {
  text: string;
  className?: string;
}
const { text, className } = Astro.props as CaptionProps;
---

<span class=`font-lexend text-sm font-regular w-full ${className}`>{text}</span>
