---
interface TitleProps {
  text: string;
  className?: string;
}
const { text, className } = Astro.props as TitleProps;
---

<h1 class=`font-lexend text-4xl font-bold w-full ${className}`>{text}</h1>
